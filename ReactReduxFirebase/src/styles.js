import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  toolbarNavigationBarStyle: {
    backgroundColor: "#f2903d"
  },
  toolbarTitleStyle: {
    color: "#fff"
  }
});

export default styles;
